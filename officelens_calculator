<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Rodenstock OfficeLens Calculator</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
:root{
  --navy:#0F1B3C;
  --navy-2:#182A52;
  --blue:#0A4A82;
  --teal:#5FC9BB;
  --teal-dark:#3B9E92;
  --paper:#FFFFFF;
  --cloud:#F4F6F8;
  --ink:#131A2E;
  --muted:#66707E;
  --line:#DCE2E8;
  --line-dark:#2A3B66;
}

*{box-sizing:border-box}
html{-webkit-font-smoothing:antialiased}
body {
  margin: 0;
  background: var(--cloud);
  color: var(--ink);
  font-family: 'Inter', Arial, sans-serif;

  transform: scale(0.8);
  transform-origin: top left;
  width: 125%;
}

#backgroundSlideshow {	
  position: fixed ;
  top: 20%; 
  left: 1%;
  width: 98%;
  height: auto;
  overflow: visible;
  pointer-events: none;
}

.bg-image {
  position: fixed;
  top: 0;
  left: 1%;
  width: 98%;
  height: auto;
  object-fit: contain;

  opacity: 0;
  transition: opacity 3s ease-in-out;
}

.bg-image.active {
  opacity: 1;
}


h1,h2,h3{font-family:'Space Grotesk',Arial,sans-serif;margin:0}

/* ---------- HERO ---------- */
.hero{
  background:linear-gradient(150deg,var(--navy) 0%,var(--navy-2) 55%,var(--blue) 100%);
  color:#fff;
  padding:52px 24px 64px;
  position:relative;
  overflow:hidden;
}
.hero::after{
  content:"";
  position:absolute;
  right:-120px;
  top:-140px;
  width:420px;
  height:420px;
  border-radius:50%;
  border:1px solid rgba(95,201,187,.35);
}
.hero::before{
  content:"";
  position:absolute;
  right:-40px;
  top:40px;
  width:260px;
  height:260px;
  border-radius:50%;
  background:radial-gradient(circle at 30% 30%, rgba(95,201,187,.25), transparent 65%);
}
.hero-inner{max-width:1240px;margin:auto;position:relative}
.eyebrow-line{
  display:flex;align-items:center;gap:10px;
  color:var(--teal);font-weight:600;font-size:13px;
  letter-spacing:.03em;margin-bottom:18px;
}
.eyebrow-line span.dot{width:7px;height:7px;border-radius:50%;background:var(--teal);display:inline-block}
.hero h1{
  font-size:clamp(32px,4.4vw,52px);
  font-weight:700;
  line-height:1.05;
  max-width:15ch;
  letter-spacing:-.01em;
}
.hero p{
  margin:16px 0 0;
  color:#C9D3E4;
  font-size:16px;
  max-width:46ch;
  line-height:1.55;
}

/* ---------- LAYOUT ---------- */
.wrap{
  max-width:1240px;
  margin:-34px auto 60px;
  padding:0 24px;
  position:relative;
  display:block;
}

/* ---------- FILTER PANEL ---------- */
.panel {
  background: rgba(255, 255, 255, 0.5);
  border: 1px solid var(--line);
  border-radius: 18px;
  padding: 20px 22px;
  box-shadow: 0 18px 40px -20px rgba(15,27,60,.25);
  margin-bottom: 24px;
}

.panel .filter{
  margin-bottom:16px;
}

.panel .filter:last-of-type{
  margin-bottom:0;
}
.filter{margin-bottom:22px}
.filter:last-of-type{margin-bottom:0}
.filter h3{
  flex:0 0 100px;
  margin-top:3%;
  margin-bottom:1%;

}

.filter .buttons{
  flex:1;
}
.buttons{
  display:flex;
  flex-wrap:wrap;
  gap:7px;
}
.btn{
  border:1.5px solid var(--line);
  background:#fff;
  border-radius:20px;
  padding:8px 14px;
  cursor:pointer;
  font-weight:600;
  font-size:13.5px;
  color:var(--navy);
  transition:.15s ease;
  font-family:'Inter',Arial,sans-serif;
}
.btn:hover{border-color:var(--teal-dark);color:var(--teal-dark)}
.btn.active{
  background:var(--navy);
  border-color:var(--navy);
  color:#fff;
}
.btn.active.teal{background:var(--teal-dark);border-color:var(--teal-dark)}

.search-box{
  flex:1;
  width:100%;
}

.search{
  width:100%;
  border:1.5px solid var(--line);
  border-radius:12px;
  background:var(--cloud);
  padding:10px 14px;
  font-size:14px;
  outline:none;
  font-family:'Inter',Arial,sans-serif;
  color:var(--ink);
}
.search:focus{border-color:var(--blue);background:#fff}

.note{
  padding:12px 14px;
  border-radius:12px;
  background:#EAF6F4;
  border:1px solid #C9EAE4;
  color:var(--navy);
  font-weight:600;
  font-size:13.5px;
  margin-top:18px;
}





.attribute-guide{
  margin:1% 0 1%;
  padding:16px 18px;
  background:var(--cloud);
  border:1px solid var(--line);
  border-radius:14px;
}

.attribute-guide-title{
  display:flex;
  align-items:baseline;
  justify-content:space-between;
  gap:12px;
  margin-bottom:12px;
}

.attribute-guide-title span{
  font-family:'Space Grotesk',Arial,sans-serif;
  font-size:14px;
  font-weight:700;
  color:var(--navy);
}

.attribute-guide-title small{
  font-size:10.5px;
  color:var(--muted);
}

.attribute-buttons{
  display:flex;
  flex-direction:column;
  gap:12px;
}

.attribute-category{
  display:flex;
  align-items:center;
  gap:12px;
}

.attribute-category-title{
  flex:0 0 120px;

  font-size:10px;
  font-weight:700;
  text-transform:uppercase;
  letter-spacing:.06em;

  color:var(--muted);
}

.attribute-category-buttons{
  display:flex;
  flex-wrap:wrap;
  gap:7px;
}

.attribute-btn{
  border:1px solid var(--line);
  background:var(--paper);
  color:var(--navy);
  border-radius:20px;
  padding:7px 12px;
  cursor:pointer;

  font-family:'Inter',Arial,sans-serif;
  font-size:11px;
  font-weight:700;

  transition:.15s ease;
}

.attribute-btn:hover{
  border-color:var(--teal-dark);
  color:var(--teal-dark);
}

.attribute-btn.active{
  background:var(--navy);
  border-color:var(--navy);
  color:#fff;
}


/* ---------- DESCRIPTION ---------- */

.attribute-description{
  margin-top:12px;
  padding:3%;

  background:var(--paper);
  border:1px solid var(--line);
  border-radius:12px;

  min-height:5%;

  transition:opacity .15s ease;
}

.attribute-placeholder{
  color:var(--muted);
  font-size:11px;
  text-align:center;
  padding:5px 0;
}


/* Description content */

.attribute-name{
  display:flex;
  align-items:center;
  gap:9px;
  margin-bottom:8px;
}

.attribute-code{
  display:inline-flex;
  align-items:center;
  justify-content:center;

  padding:4px 7px;
  border-radius:6px;

  background:var(--navy);
  color:#fff;

  font-size:10px;
  font-weight:700;
}

.attribute-full-name{
  font-family:'Space Grotesk',Arial,sans-serif;
  font-size:14px;
  font-weight:700;
  color:var(--navy);
}

.attribute-details{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:12px;
}

.attribute-detail{
  display:flex;
  flex-direction:column;
  gap:3px;
}

.attribute-detail-label{
  font-size:9px;
  text-transform:uppercase;
  letter-spacing:.07em;
  font-weight:700;
  color:var(--muted);
}

.attribute-detail-text{
  font-size:11px;
  line-height:1.4;
  color:var(--ink);
}






/* ---------- RESULTS ---------- */
.results-col{
  min-width:0;
}

.status{
  display:flex;
  justify-content:space-between;
  align-items:baseline;
  margin:0 0 16px;
  color:var(--muted);
  font-size:14px;
}

.count{
  font-weight:700;
  color:var(--ink);
  font-family:'Space Grotesk',Arial,sans-serif;
}

.results{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:16px;
  align-items:start;
}


/* ---------- CARD ---------- */

.card{
  background:var(--paper);
  border:1px solid var(--line);
  border-radius:16px;
  padding:20px;
  position:relative;
  overflow:hidden;
  transition:
    border-color .15s ease,
    transform .15s ease,
    padding .15s ease;
}

.card::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  bottom:0;
  width:4px;
  background:linear-gradient(180deg,var(--teal),var(--blue));
}

.card:hover{
  border-color:var(--blue);
}


/* ---------- CARD HEADER ---------- */

.card-header{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:10px;
}

.card h2{
  font-size:18px;
  font-weight:600;
  margin:0 0 4px;
  color:var(--navy);
  line-height:1.25;
}

.card-toggle{
  flex-shrink:0;
  cursor:pointer;
}


/* ---------- CARD CONTENT ---------- */

.card-content{
  display:block;
}


/* ---------- MINIMIZED ---------- */

.card.minimized{
  height:auto !important;
  min-height:0 !important;
  padding:12px 20px !important;
  align-self:start;
}

.card.minimized .card-content{
  display:none !important;
}

.card.minimized .card-header{
  margin-bottom:0;
}

.card.minimized h2{
  margin:0;
}


/* ---------- OTHER CARD ELEMENTS ---------- */

.type-tag{
  display:inline-block;
  font-size:10.5px;
  font-weight:700;
  letter-spacing:.05em;
  color:var(--teal-dark);
  background:#EAF6F4;
  border-radius:6px;
  padding:3px 8px;
  margin-bottom:10px;
}

.meta{
  font-size:12.5px;
  color:var(--muted);
  margin-bottom:14px;
}

.prices{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:7px;
}

.price{
  border:1px solid var(--line);
  border-radius:10px;
  padding:9px 4px;
  text-align:center;
  background:var(--cloud);
}

.price b{
  display:block;
  font-size:10px;
  color:var(--muted);
  margin-bottom:4px;
  font-weight:700;
  letter-spacing:.02em;
}

.price span{
  font-weight:700;
  font-size:13.5px;
  color:var(--ink);
}

.na{
  color:#B7BEC8;
}

.tech{
  display:flex;
  flex-direction:column;
  gap:10px;
  margin-top:16px;
}

.tech-group{
  display:flex;
  align-items:center;
  flex-wrap:wrap;
  gap:6px;
}

.tech-label{
  width:100%;
  font-size:9.5px;
  font-weight:700;
  letter-spacing:.08em;
  text-transform:uppercase;
  color:var(--muted);
  margin-bottom:1px;
}

.tech-items{
  display:flex;
  flex-wrap:wrap;
  gap:6px;
}

.badge{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-width:40px;
  height:26px;
  padding:0 9px;
  border-radius:14px;
  background:var(--navy);
  color:#fff;
  font-size:10.5px;
  font-weight:700;
  letter-spacing:.02em;
  transition:opacity .15s ease, background .15s ease;
}
.badge.muted{
  background:transparent;
  color:var(--muted);
  border:1px solid var(--line);
  opacity:.95;
}

.badge.fdt{
  background:var(--teal-dark);
}

.badge.fdt.muted{
  background:transparent;
  color:var(--muted);
  border:1px solid var(--line);
  opacity:.95;
}

.loading,.empty{
  text-align:center;
  padding:20px 20px;
  color:var(--muted);
  background:var(--paper);
  border:1px dashed var(--line);
  border-radius:16px;
  grid-column:1/-1;
  font-size:14.5px;
}
.error{
  color:#9b1c1c;
  background:#FBEAE8;
  border:1px solid #F2C9C4;
  padding:16px;
  border-radius:12px;
  grid-column:1/-1;
}

@media(max-width:860px){

  .wrap{
    margin-top:-24px;
  }

  .panel{
    position:relative;
    top:0;
  }

  .hero{
    padding:40px 20px 52px;
  }

}


@media(max-width:560px){
  .results{grid-template-columns:1fr}
  .prices{grid-template-columns:repeat(2,1fr)}
  .attribute-details{
    grid-template-columns:1fr;
    gap:8px;
  }

  .attribute-guide-title{
    align-items:flex-start;
    flex-direction:column;
    gap:3px;
  }


}



</style>
</head>

<body>


<div id="backgroundSlideshow">
  <img style="margin-top:26%; border-radius:2%" class="bg-image active" src="https://sp-rx.github.io/rodenstock/bg1.jpg">
  <img style="margin-top:26%; border-radius:2%" class="bg-image" src="https://sp-rx.github.io/rodenstock/bg2.jpg">
  <img style="margin-top:26%; border-radius:2%" class="bg-image" src="https://sp-rx.github.io/rodenstock/bg3.jpg">
  <img style="margin-top:26%; border-radius:2%" class="bg-image" src="https://sp-rx.github.io/rodenstock/bg4.jpg">
</div>

<script>
document.addEventListener("DOMContentLoaded", function () {

  const bgImages = document.querySelectorAll("#backgroundSlideshow .bg-image");

  if (!bgImages.length) return;

  let bgIndex = 0;

  function nextBackground() {
    bgImages[bgIndex].classList.remove("active");

    bgIndex = (bgIndex + 1) % bgImages.length;

    bgImages[bgIndex].classList.add("active");
  }

  // First change after 3 seconds
  setTimeout(function () {

    nextBackground();

    // Continue every 6 seconds
    setInterval(nextBackground, 6000);

  }, 3000);

});
</script>



<div class="hero">
  <div class="hero-inner">
    <div class="eyebrow-line"><span class="dot"></span>Lens Finder</div>
    <h1>Calculator for Office Lens Design.</h1>
    <p>Rodenstock's Office Lens.</p>
  </div>
</div>


<div class="wrap">
<div style="margin-top:6%"></div>
  <div class="panel">

    <div class="filter">


<div class="box">


<div class="instructions">
    <b>How to use</b><br>
    <b>Step 1.</b> Determine your Cross Power via refraction.<br>
    <b>Step 2.</b> Determine your Near Power via refraction.<br>
    <b>Step 3.</b> Choose your type of design.<br>
    <b>Step 4.</b> Follow the ordering details on the right.
</div>

<div class="inputs">
<div>Eye Point / Cross RX - Fill in the Final RX for the Intermediate Vision </div>

<div style="margin-top:2%" class="row">
<div><label>Sphere</label><input id="s" type="number" step=".25" value="1.50"></div>
<div><label>Cylinder</label><input id="c" type="number" step=".25" value="-1.00"></div>
<div><label>Axis</label><input id="a" type="number" min="0" max="180" value="90"></div>
<div><label>Add At The Near</label><input id="add" type="number" step="0.25" value="1.25"></div>
<button onclick="search()">Search</button>
<div>

<button onclick="sortResults('add')">Sort by Near Zone</button>
<button onclick="sortResults('minus')">Sort by Above the Cross</button>
<button onclick="sortSlope()">Sort by Slope</button>
</div>
</div>
<div style="margin-top:2%"><div>



<button onclick="setTolerance(.130)">±0.130 D</button>
<button onclick="setTolerance(.060)">±0.060 D</button>
<button onclick="setTolerance(.030)">±0.030 D</button><br><br>

<span>Matching range: ± <span id="toleranceDisplay">0.13</span> D</span>

</div></div>


</div>



<div id="results"></div>

</div>

<script>
const data=[
["BOOK +1.25",.41,-.34],
["BOOK +1.50",.50,-.41],
["BOOK +1.75",.58,-.47],
["BOOK +2.00",.66,-.54],
["BOOK +2.25",.74,-.61],
["BOOK +2.50",.83,-.68],
["BOOK +2.75",.91,-.74],
["BOOK +3.00",.99,-.81],

["PC +1.25",.63,-.25],
["PC +1.50",.75,-.30],
["PC +1.75",.88,-.35],
["PC +2.00",1,-.40],
["PC +2.25",1.13,-.45],
["PC +2.50",1.25,-.50],
["PC +2.75",1.38,-.55],
["PC +3.00",1.50,-.60],

["ROOM +1.25",.75,-.31],
["ROOM +1.50",.90,-.38],
["ROOM +1.75",1.05,-.44],
["ROOM +2.00",1.20,-.50],
["ROOM +2.25",1.35,-.56],
["ROOM +2.50",1.50,-.63],
["ROOM +2.75",1.65,-.69],
["ROOM +3.00",1.80,-.75]
];

const wd=n=>``;
const f=n=>(n>=0?"+":"")+n.toFixed(2);
const rx=(s,c,a)=>`${f(s)} ${f(c)} × ${a}°`;

let found=[];
let tolerance=.13;



function search(){
    let s=+document.getElementById("s").value;
    let target=+document.getElementById("add").value;

    found=data
        .map(x=>({...x,d:Math.abs(x[1]-target)}))
.filter(x=>x.d<=tolerance);

    sortResults("add");
}

function sortResults(type){
    found.sort((x,y)=>
        type==="add" ? x[1]-y[1] : x[2]-y[2]
    );
    displayResults();
}

function sortSlope(){
    found.sort((x,y)=>
        (Math.abs(x[1])+Math.abs(x[2]))-
        (Math.abs(y[1])+Math.abs(y[2]))
    );
    displayResults();
}

function sortSlope(){
    found.sort((x,y)=>
        (Math.abs(x[1])+Math.abs(x[2]))-
        (Math.abs(y[1])+Math.abs(y[2]))
    );
    displayResults();
}


function slope(x){
    return `<span>${(Math.abs(x[1])+Math.abs(x[2])).toFixed(2)}</span>`;
}

function setTolerance(value){
    tolerance=value;
    document.getElementById("toleranceDisplay").textContent=value.toFixed(3);
    search();
}


function displayResults(){
    let s=+document.getElementById("s").value;
    let c=+document.getElementById("c").value;
    let a=+document.getElementById("a").value;

    document.getElementById("results").innerHTML=found.length
    ? found.map(x=>{

        let productPower=parseFloat(x[0].split(" ")[1]);
        let addSphere=s+x[1];
        let farSphere=Math.round((addSphere-productPower)*4)/4;

        return `
        <div class="result">

            <div class="info">
                <div class="product">${x[0]}, Slope : ${slope(x)}</div>

                <div class="rx">
                    <b>Above the Cross ${wd(x[3])}:</b>
                    ${rx(s+x[2],c,a)}
                    <span>(${f(x[2])})</span>
                </div>

                <div class="rx">
                    <b>RX At the Cross ${wd(x[4])}:</b>
                    ${rx(s,c,a)}
                </div>

                <div class="rx">
                    <b>At the Near Zone ${wd(x[5])}:</b>
                    ${rx(addSphere,c,a)}
                    <span>(${f(x[1])})</span>
                </div>
            </div>

            <div class="far">
                <div class="rx">
                    <b><u>Order The Lens Like This:</u></b>
                </div>

                <div class="rx">
                    <b>Far Power:</b> ${rx(farSphere,c,a)}
                </div>

                <div class="rx">
                    <b>Add Type & RX:</b> ${x[0]}
                </div>
            </div>

        </div>`;
    }).join("")
    : `<div class="result">No matching products.</div>`;
}

document.addEventListener("keydown",e=>{
    if(e.key==="Enter")search();
});

search();
</script>

<style>
*{box-sizing:border-box}
.box{}
.inputs,.result{background:#fff;padding:18px;border-radius:10px;margin-bottom:12px}
.row{display:flex;gap:10px;align-items:end;flex-wrap:wrap}
label{display:block;font-size:12px;margin-bottom:4px}
input{width:100px;padding:9px;font-size:16px}
button{padding:10px 20px;border:0;border-radius:6px;background:#333;color:#fff;cursor:pointer}
.sort{display:flex;gap:8px;margin-bottom:12px}
.result{display:flex;justify-content:space-between;align-items:center;gap:20px}
.info{flex:1}
.product{font-size:20px;font-weight:bold;margin-bottom:10px}
.rx{margin:5px 0}
.small{font-size:13px;color:#666}
.far{min-width:190px;padding-right:10%}
.far-title{display:inline;font-size:inherit;font-weight:bold;margin:0}
.far-rx{display:inline;font-size:inherit;font-weight:normal}
.instructions{
    background:#fff;
    padding:14px 18px;
    border-radius:10px;
    margin-bottom:12px;
    line-height:1.6;
}

</style>





</div>
</div>
</div>
</div>


</body>
</html>

